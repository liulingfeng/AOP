apply plugin: 'groovy'
apply plugin: 'maven'

repositories {
    mavenCentral()
    jcenter()
}

dependencies {
    compile gradleApi()
    compile localGroovy()
    compile 'com.android.tools.build:gradle:3.4.2'
}

group='com.lxs.plugin'
version='0.0.3'

uploadArchives{
    repositories{
        mavenDeployer{
            repository(url: uri('./my-plugin'))
        }
    }
}